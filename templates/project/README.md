# Ebook translation project

This project uses the user skill `ebook-translation-zh` and the canonical D-drive toolkit. Read `.ebook-translation/project.yaml`, then run the lightweight wrapper in `scripts/`. Book-specific paragraph, image, and terminology overrides live under `.ebook-translation/`.
